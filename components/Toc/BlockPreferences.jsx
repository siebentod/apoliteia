import { PreferencesButton } from './SVG';

function BlockPreferences() {
  return (
    <>
      <button
        className={'leftPanelButton preferencesButton'}
        // onClick={handleTransButton}
      >
        <PreferencesButton />
      </button>
    </>
  );
}

export default BlockPreferences;
